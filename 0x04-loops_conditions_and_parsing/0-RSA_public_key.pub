#!/usr/bin/env bash
# This script generates a SSH RSA key pair

# Generate RSA key pair
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"

# Output public key
cat ~/.ssh/id_rsa.pub > 0-RSA_public_key.pub
